
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.io.ByteArrayOutputStream;
import java.io.PrintStream;

import static org.junit.jupiter.api.Assertions.*;

class UserInterfaceTest {
    /*
    UserInterface userInterface = new UserInterface();

    @DisplayName("Prints 'Start'")
    @Test
    void start() throws Exception{
        ByteArrayOutputStream outContent = new ByteArrayOutputStream();
        System.setOut(new PrintStream(outContent));
        userInterface.initializing();
        String expectedOutput = "The application i starting..." +
                "\n---------------------------------------------------------------------------------------";
        assertEquals(expectedOutput, outContent.toString().trim().replace("\r",""));
    }

    @Test
    @DisplayName("Created instance of user controller")
    void ControllerNotNull(){
        assertNotNull(userInterface.controller);
    }*/
}