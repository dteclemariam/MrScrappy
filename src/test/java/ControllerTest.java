import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.openqa.selenium.WebDriver;

import java.util.ArrayList;
import static org.junit.jupiter.api.Assertions.*;

@DisplayName("Controller should")

class ControllerTest {
/*
    Controller controller = new Controller();

    //Test to see that the instances has been created
    @DisplayName("Creates instances of classes")
    @Test
    void createHelperClasses(){
        assertAll(
                () -> assertNotNull(controller.getAppVariable()),
                () -> assertNotNull(controller.getResources())
        );
    }

    @DisplayName("Get instance of webdriver from Resources")
    @Test
    void getWebdriver(){
        controller.getWebdriverFromResources();
        assertNotNull(controller.getDriver());
        controller.closeWebdriver();
    }


    @DisplayName("Webpages containes the 3 URL's")
    @Test
    void webpagesContainsAtLeastOneUrl(){
        controller.getWebpagesFromResource();
        ArrayList <String> webpages = controller.getWebpages();
        assertNotNull(webpages);
    }*/
}
